@inherits BaseInput;

@using Microsoft.CST.OAT.Utils;
@using System;

<input class="form-control" id="@id" type="text" @bind="SubProperty" data-input-type="ulong-input" disabled="@isDisabled"/>

@code {
    public string SubProperty
    {
        get
        {
            if (Helpers.GetValueByPropertyOrFieldName(Object, SubPath) is ulong val)
            {
                return val.ToString();
            }
            return string.Empty;
        }
        set
        {
            if (ushort.TryParse(value, out ushort val))
            {
                Helpers.SetValueByPropertyOrFieldName(Object, SubPath, val);
                onChangeAction.Invoke();
            }
        }
    }
}