@using Microsoft.CST.OAT.Utils;
@using System;

<input class="form-control" id="@id" type="text" @bind="SubProperty" data-input-type="ushort-input"/>

@code {
    [Parameter]
    public object? Object { get; set; }

    [Parameter]
    public string? SubPath { get; set; }

    [Parameter]
    public string id { get; set; } = string.Empty;

    public string SubProperty
    {
        get
        {
            var val = Helpers.GetValueByPropertyOrFieldName(Object, SubPath);
            if (val != null)
            {
                return val.ToString();
            }
            return string.Empty;
        }
        set
        {
            if (int.TryParse(value, out int intVal))
            {
                Helpers.SetValueByPropertyOrFieldName(Object, SubPath, intVal);
            }
        }
    }
}