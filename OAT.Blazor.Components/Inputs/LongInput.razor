@using Microsoft.CST.OAT.Utils;
@using System;

<input class="form-control" id="@id" type="text" @bind="SubProperty" data-input-type="long-input"/>

@code {
    [Parameter]
    public object? Object { get; set; }

    [Parameter]
    public string? SubPath { get; set; }

    [Parameter]
    public string id { get; set; } = string.Empty;

    public string SubProperty
    {
        get
        {
            if (Helpers.GetValueByPropertyOrFieldName(Object, SubPath) is long val)
            {
                return val.ToString();
            }
            return string.Empty;
        }
        set
        {
            if (long.TryParse(value, out long longVal))
            {
                Helpers.SetValueByPropertyOrFieldName(Object, SubPath, longVal);
            }
        }
    }
}